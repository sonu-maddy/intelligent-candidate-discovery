pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
PyYAML>=6.0

# Optional (only needed if llm_rerank.enabled: true in config.yaml)
# anthropic>=0.40

# Optional (only needed to run the Streamlit demo app)
# streamlit>=1.30
